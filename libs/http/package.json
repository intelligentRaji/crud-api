{
  "name": "@repo/http",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build && tsc -p tsconfig.json && tsc-alias --project tsconfig.json",
    "preview": "vite preview"
  },
  "dependencies": {
    "@repo/core": "workspace:*",
    "@repo/common": "workspace:*"
  },
  "devDependencies": {
    "tsc-alias": "^1.8.16"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  }
}
